# Screenshots

Add the following screenshots for release distribution:

- `menu-popover.png` — Menu bar popover showing status + quick settings
- `overlay-alert.png` — Full-screen alert overlay ("MOVE! MOVE! MOVE!")
- `break-countdown.png` — Break countdown card with progress
- `completion-state.png` — Success/complete state

Recommended size: 1440×900 (or native display size), PNG format.
